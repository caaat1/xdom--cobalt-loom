import type { TypeDescription } from '../../../../../type/description/type.js'

export const atomUnionTypeDescription: TypeDescription = `AtomUnion is either an Atom or a shorthand for an Atom (AtomShorthand).`
